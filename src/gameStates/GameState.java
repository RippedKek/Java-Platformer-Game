package gameStates;

public enum GameState {

	PLAYING, MENU, OPTIONS, ACHIEVEMENTS, ENCYCLOPEDIA, QUIT, PLAYER_INFO, ENEMY_INFO, OBJECT_INFO, STARTUP;
	
	public static GameState state = STARTUP;
	
}
